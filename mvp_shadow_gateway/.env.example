# Copie para .env e ajuste conforme ambiente (não comitar este arquivo).

# PLC / Modbus
MB_HOST=192.168.1.121
MB_UNIT=1

# InfluxDB v2
INFLUX_USER=admin
INFLUX_PASS=<definir-no-setup>
INFLUX_ORG=retrofit4
INFLUX_BUCKET=mvp
INFLUX_TOKEN=<preencher-pelo-setup>

# Grafana
GRAFANA_USER=admin
GRAFANA_PASS=<definir-no-setup>

# Tags de contexto
SITE=Bento
LINHA=Seccionadora01
MAQUINA=Maquina01

# Paradas
STOP_THRESHOLD=120

# MQTT (opcional)
MQTT_ENABLE=false
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_TOPIC_PREFIX=oee/${SITE}/${LINHA}/${MAQUINA}
